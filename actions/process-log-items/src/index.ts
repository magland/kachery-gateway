// import { DocumentSnapshot } from '@google-cloud/firestore'

const processLogItems = async () => {
    console.info(`TESTVAR: ${process.env["TESTVAR"]}`)
    console.info('process-log-items:::')
}

processLogItems()